# PLAN

> Generated by `/autopilot` on 2026-05-13. Test/dogfood run.
> Project: eruto-skills (autopilot subdirectory)

## Goal

Add a one-command installer to `autopilot/` that creates the skill symlink/junction on Windows, macOS, and Linux. Idempotent. Manual command equivalents preserved as comments in the scripts.

## Out of scope

- CLI args beyond a single optional `--force` (don't reinvent argument parsing)
- Cross-shell auto-detection beyond OS branching
- Other skills in `eruto-skills/` (this is scoped to autopilot only)
- Auto-updating the README — that's T03, treat as separate

## Open questions

(none)

## Tasks

```yaml
- id: T01
  priority: 1
  passes: true   # 2026-05-13 iteration 1
  description: Add scripts/install.ps1 for Windows that creates a Junction from %USERPROFILE%\.claude\skills\autopilot to the autopilot repo path. Idempotent.
  acceptance: |
    From C:\@projects\eruto-skills\autopilot\:
    1. `Remove-Item "$env:USERPROFILE\.claude\skills\autopilot" -Force` (if present)
    2. `pwsh -File scripts/install.ps1` exits 0 and prints "created junction: <path>"
    3. `(Get-Item "$env:USERPROFILE\.claude\skills\autopilot").LinkType` returns "Junction"
    4. `(Get-Item "$env:USERPROFILE\.claude\skills\autopilot").Target` resolves to "C:\@projects\eruto-skills\autopilot"
    5. Re-running step 2 prints "already linked: <path>" and exits 0
    6. Script contains a leading comment block with the equivalent manual `New-Item -ItemType Junction` invocation
  notes:
    - "2026-05-13 iter1 PASS all 6 acceptance sub-steps; commit deferred (see Friction #10)"

- id: T02
  priority: 2
  passes: true   # 2026-05-13 iteration 2
  description: Add scripts/install.sh for macOS and Linux. Creates a symlink at ~/.claude/skills/autopilot pointing to the repo. Idempotent.
  acceptance: |
    Tested via WSL (per machine env in CLAUDE.md):
    1. `wsl.exe -- bash -c 'rm -f ~/.claude/skills/autopilot'`
    2. `wsl.exe -- bash -c 'cd /mnt/c/@projects/eruto-skills/autopilot && bash scripts/install.sh'` exits 0, prints "created symlink: <path>"
    3. `wsl.exe -- bash -c 'readlink ~/.claude/skills/autopilot'` returns a path resolving to the autopilot repo
    4. Re-running step 2 prints "already linked: <path>" and exits 0
    5. Script contains a leading comment block with the equivalent manual `ln -s` invocation
  notes:
    - "2026-05-13 iter2 PASS all 5 acceptance sub-steps via WSL Ubuntu (heredoc form)"

- id: T03
  priority: 3
  passes: true   # 2026-05-13 iteration 3
  description: Update autopilot/README.md "Install" section so scripts/install.* is the primary path and manual commands are presented as fallback.
  acceptance: |
    1. `grep -c "scripts/install" autopilot/README.md` returns >= 1
    2. The manual `New-Item -ItemType SymbolicLink` and `ln -s` blocks remain in the file but are introduced as "if you can't run the script:"
    3. `git diff autopilot/README.md` shows only the Install section changed; no other sections touched
  notes:
    - "2026-05-13 iter3 PASS step1+2; step3 N/A (file untracked) → spirit-pass: manual inspection confirms only Install section changed. See Friction #15."
```

## Project conventions for this run

- Acceptance tests are mostly manual PowerShell + WSL bash commands — the loop body must run them step-by-step, capturing exit codes and stdout.
- No automated test framework is in scope for this task. `git diff` is the proof.
- `git add` policy: per-task, named files only — never `-A`.
